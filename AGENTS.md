# AstroBlog 项目规范与 AI 协作准则 (AGENTS.md)

本文档定义了本项目中的核心开发规范、Notion 笔记同步流程、图床管理方案以及 AI 助手的协作准则。任何 AI 助手在本项目中工作时均须严格遵守。

---

## 1. 核心协作准则（先商量，再动手）

- **区分「疑问探讨」与「执行指令」**：
  - 当用户提出疑问、咨询技术原理、询问排版原因或探讨可行性时，AI 只负责分析原因并提供可选方案与利弊对比，**严禁擅自修改文件、改动配置或执行系统操作**。
- **方案沟通与确认机制**：
  - 任何涉及代码修改、样式配置、文章生成、图床迁移或 Git 推送的动作，必须**先向用户汇报具体实施方案**，在得到用户**明确确认（如“去改吧”、“同意”、“确认”）**后，方可开始动手执行。

---

## 2. Notion 笔记同步与排版自动化规范

从 Notion 导入或同步文章到博客时（如面试题、八股文、技术笔记），须遵循以下 **4 重前置清洗规则**，确保网页端排版与 Notion 视觉效果 1:1 精准一致：

### (1) 表格全自动转标准 Markdown
- **禁止保留原始 HTML <table> 标签**；
- 必须将其解析重构为标准 Markdown 表格语法（| 表头1 | 表头2 | ... |）；
- 确保表格能被主题的 .table-wrapper 容器包裹，具备圆角边框及自适应平滑横向滚动支持。

### (2) 列表边界隔离（防止错误向右缩进）
- 在任何无序列表（- ...）或有序列表（1. ...）结束后，若紧接着普通段落、加粗提问（如 **那出现...？**）或小标题，**必须强制插入双换行（\n\n）**；
- 确保 <ul>/<ol> 列表正确闭合，后续文本恢复顶格左对齐（0 缩进）。

### (3) 多段分行与换行保真
- 避免因 Markdown 单次回车被合并为同一行的问题，在多行段落与问答解答之间必须保留标准空行（\n\n），保证分行清晰。

### (4) 独立图片块级居中排版
- 每张图片 ![](...) 上下均须保留空行；
- 图片默认以块级元素全宽居中展示（display: block; margin: 1.5rem auto; max-width: 100%），严禁横向压缩挤压。

---

## 3. GitHub 专用图床与 jsDelivr CDN 规范

为了防止博客代码仓库体积膨胀，所有文章插图与架构图均托管在专用图床仓库中。

- **图床仓库信息**：
  - 仓库地址：https://github.com/Cxhahalala/hexo-images-1.git
  - 默认分支：main
  - 图片存放路径：images/
- **防重复与唯一性机制（MD5 内容哈希）**：
  - 所有新图片下载后，必须基于图片完整的二进制字节流计算 **16 位小写十六进制 MD5 哈希** 命名（如 325854c9df94bdad.png）；
  - 相同图片自动去重，不同图片绝不重名，且与老图床的日期时间戳命名天然隔离，**绝对禁止覆盖已有图片**。
- **CDN 链接格式**：
  - 文章中的图片链接统一替换为：
    https://cdn.jsdelivr.net/gh/Cxhahalala/hexo-images-1@main/images/<hash>.png
- **本地网络代理**：
  - Git 推送或拉取图床仓库时，如果遇到网络阻碍，使用本地代理：-c http.proxy=http://127.0.0.1:7897。

---

## 4. 文章目录与分类结构

- **面试题与八股文文章**：
  - 文件路径：src/content/posts/interview/<slug>.md
  - 统一 Frontmatter：
    category: "面试"
    tags: ["Java", "分类标签", "面试"]
    draft: false

---

## 5. 构建校验与部署流程

1. **语法与类型校验**：
   - 每次生成或修改文章后，必须运行 pnpm check，确保 0 errors, 0 warnings。
2. **Git 提交与 Cloudflare 部署**：
   - 博客主仓库分支为 master，提交并推送至 https://github.com/Cxhahalala/AstroBlog.git；
   - 由 Cloudflare Pages 自动监听 master 分支并完成全球 CDN 部署。
